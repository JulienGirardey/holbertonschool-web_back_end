export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

  if (trueOrFalse) {
	// Intentionally left blank for block scope demonstation
  }

  return [task, task2];
}
